<!DOCTYPE html>
<html lang="en">
  {% embed "partials/head.twig" with { title: 'Topics', locale: locale } %}
  {% endembed %}

  <body>
    {% embed "partials/masthead.twig" with { locale: locale } %}
    {% endembed %}

    <h1 class="title title--headline title--section wings"><span class="gutters">Topics</span></h1>

    <ul class="wings">
      <li class="gutters">
          <a class="title anchor--shy" href="{{ route('youth', {}) }}">Youth in agriculture</a>
      </li>
      <li class="gutters">
          <a class="title anchor--shy" href="{{ route('woman', {}) }}">The role of women in Africa</a>
      </li>
    </ul>
  </body>
</html>
